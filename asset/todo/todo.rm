# Todo List

## Minor
- Tweak the style of the `import` field type.
- Show an error when the import submit button is pressed without setting a file.

## Major
- Support namespaces.
- Separate resources from PHP files.
- Add the ability to auto-load file constructs.
- Add the ability to set slug that suppresses the class name used for the hook names.